namespace LinqNetCore.Processors
{
    public static class Playground
    {


        //public static IEnumerable<T> Filter<T>(this IEnumerable<T> source, Func<T, bool> predicate)
        //{
        //    foreach (var item in source)
        //    {
        //        if (predicate(item))
        //        {
        //            yield return item;
        //            //yield return item;
        //        }
        //    }
        //}



        //public static IEnumerable<T> Filter1<T>(this IEnumerable<T> source, Func<T, bool> predicate)
        //{
        //    var list = new List<T>();
        //    foreach (var item in source)
        //    {
        //        if (predicate(item))
        //        {
        //            list.Add(item);
        //            //yield return item;
        //        }
        //    }
        //    return list;
        //}
    }
}
